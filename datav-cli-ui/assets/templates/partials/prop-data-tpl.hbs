{{#each configs as | item |}}
{{#unless (isEmptyArray item.config.pairs)}}
const pairData{{item.config.id}} = [
  {{#each config.pairs as | pair |}}
  { key: '{{pair.key}}', value: '{{pair.value}}' },
  {{/each}}
]

{{/unless}}
{{#unless (isEmptyArray item.config.enums)}}
const enumData{{item.config.id}} = [
  {{#each config.enums as | en |}}
  '{{en}}',
  {{/each}}
]

{{/unless}}
{{#if (includes item.config.features 'add')}}
const handleAdd{{capitalize item.key}}Item = () => {
  throw new Error('handleAdd{{capitalize item.key}}Item Not Implemented')
}

{{/if}}
{{> prop-data-tpl configs=item.children}}
{{/each}}
